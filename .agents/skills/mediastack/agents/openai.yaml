interface:
  display_name: "Mediastack"
  short_description: "Repo-specific patterns and workflows for mediastack"
  default_prompt: "Use the mediastack repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true